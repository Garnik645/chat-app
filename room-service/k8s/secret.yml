apiVersion: v1
kind: Secret
metadata:
  name: room-service-secret
  namespace: chat-app
type: Opaque
# Values must be base64-encoded.
# Generate with:  echo -n 'my-value' | base64
data:
  DB_USERNAME: <base64-encoded-username>
  DB_PASSWORD: <base64-encoded-password>
